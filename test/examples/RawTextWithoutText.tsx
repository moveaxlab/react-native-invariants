import * as React from 'react';
import { View } from 'react-native';

export const RawTextWithoutText: React.FunctionComponent = () => (
  <View>I cannot do this</View>
);

RawTextWithoutText.displayName = 'RawTextWithoutText';
